name: artifact
on:
  workflow_dispatch:
jobs:
 jobs1:
  runs-on: ubuntu-latest
  steps:
  - uses: actions/checkout@v1
  - run: mkdir -p artifactU
  - run: echo hello > artifactU/world.txt
  - uses: actions/upload-artifact@v2
    with:
      name: hello-world-artifact
      path: artifactU/world.txt


