name: TEST
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest


steps:
- name: Set
- uses: actions/checkout@v1

- name: Run a multi-line script
  run: |
    ./test.sh
   