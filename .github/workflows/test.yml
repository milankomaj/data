name: TEST
on:
  pull_request:
    branches:
    - main
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest



- uses: actions/checkout@v1

- name: Run a multi-line script
  run: |
    ./test.sh
   
